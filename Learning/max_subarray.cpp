#include<bits/stdc++.h>

using namespace std;

void merge(vector<int> nums, int low, int high)
{
    int middle = (low+high)/2;

    return max()
}


int main()
{
    int n;
    vector<int> v;
    while((cin>>n) && n!=-1)
        v.push_back(n);
    merge(v, 0, v.size()-1);
    return 0;
}